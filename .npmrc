# Vercel's build image has no cmake/native toolchain, and bedrock-protocol's
# raknet-native dependency tries to compile a native addon on install. We
# force bedrock-protocol to use the pure-JS "jsp-raknet" backend instead
# (see api/fetch-pack.js), so the native build is unnecessary — skip it
# entirely to avoid the install failing.
ignore-scripts=true
